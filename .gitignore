node_modules/
dist/
.env
.env.local
incoming/

# Generated by fetch-daily-csv.cjs — a local sync cursor, not source code.
# Committing this would make one machine's "last synced block" clobber
# another's, and it's meaningless without the matching incoming/ data anyway.
scripts/.fetch-sync-state.json

# Cron/script output logs — local debugging data, not code.
logs/

# Hardhat build output — regenerated from contracts/*.sol on every compile,
# committing it just creates merge-conflict-prone noise.
artifacts/
cache/
typechain/
typechain-types/